# ── Database (PostgreSQL) ──
DB_HOST=localhost
DB_PORT=5432
DB_USER=ipcamera
DB_PASSWORD=ipcamera123
DB_NAME=ipcamera

# ── JWT ──
JWT_SECRET_KEY=change-this-secret-key-in-production-8a7f9b2c
JWT_EXPIRE_MINUTES=480

# ── SMTP Email (for OTP) ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_USE_TLS=true

# ── App ──
DEBUG=false
